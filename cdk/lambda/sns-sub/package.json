{
  "name": "sns-subsribe",
  "version": "0.1.0",
  "description": "A lambda to subsribe to a sns stream",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc -p tsconfig.build.json"
  },
  "keywords": [
    "node",
    "typescript",
    "lambda"
  ],
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0"
  }
}
