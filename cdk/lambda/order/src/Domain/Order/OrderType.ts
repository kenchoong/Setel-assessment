// This is how 1 order looks like, we defined the entity here

export type OrderEntity = {
  userId: string;
  productId: string;
  productName: string;
  totalOrderAmount: string;
  orderStatus: string;
  createdAt: string;
};
